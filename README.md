# python-playground

Generate pb files:
```
protoc --proto_path=proto --python_out=src proto/*.proto
```

Install packages:
```
pip3 install -r requirements.txt
```

Serde:
```
python3 src/main.py
```
